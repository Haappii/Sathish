# Bundled production config for the desktop installer.
# This file ships inside the packaged app (resources/app.asar).
# Override by setting APP_URL in the outer config.txt or via --url= launch arg.
APP_URL=http://56.228.9.197/
APP_URL_DEFAULT=http://56.228.9.197/
